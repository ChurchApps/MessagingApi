DELIMITER $$
CREATE PROCEDURE `cleanup`()
BEGIN
  DELETE from conversations where dateCreated<DATE_ADD(now(), INTERVAL -7 DAY);
  DELETE from connections where conversationId NOT IN (SELECT id from conversations);
  DELETE from messages where conversationId NOT IN (SELECT id from conversations);
END$$
DELIMITER ;
